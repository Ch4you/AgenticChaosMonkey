apiVersion: v1
kind: Service
metadata:
  name: agent-app
  labels:
    app: agent-app
spec:
  selector:
    app: agent-app
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  type: ClusterIP
